// Multiplayer backend URL. Leave blank for local dev (npm start serves both
// the static site and the Socket.io server from the same origin). Once the
// server is deployed (e.g. Render/Fly/Railway), set this to its https URL —
// required when the frontend is hosted separately on GitHub Pages.
window.CABO_SERVER_URL = 'https://cabo-game-server-3omd.onrender.com';
